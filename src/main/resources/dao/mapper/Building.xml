<?xml version="1.0" encoding="UTF-8" ?>
<!-- main/resources/dao/mapper/Building.xml -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.mapper.BuildingMapper">
	
	<select id="list" resultType="Building" parameterType="map">
		select sNo, sName, 
		(select count(*) from Reserve r ,Board b where r.sNo = b.sNo) 'boCnt',
		(select count(*) from Reserve r ,Building b where r.sNo = b.sNo and r.reStat = 0) 'reCnt',
		from Building

		<if test="id != null">
			where id = #{id}	
		</if>
		
	</select>
</mapper>